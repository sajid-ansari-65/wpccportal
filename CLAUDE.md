@AGENTS.md

## Git in this project

Sajid has allowed Claude to run `git commit` in this repo only. This
overrides the global "never write" rule for commits here and nowhere else.
Everything else in that rule still applies: no `push`, `merge`, `rebase`,
`tag` or `reset` — hand those over as commands for him to paste.
